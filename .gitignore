# Scratch / OS cruft at the repo root.
# Thread-specific ignores live in each subtree's own .gitignore
# (e.g. experiments/self-hosting-compilers/.gitignore).
.DS_Store
*.swp
scratch/

# wasi-sdk and other large downloaded toolchains
tools/
