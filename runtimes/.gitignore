# Cloned upstream sources (re-fetched by each runtime's build.sh at its pin)
*/src/
# Build + demo outputs
*/build/
*/devirt/
*/.shim/
# Cargo build dirs for embedder helpers (e.g. wasmi/runner)
**/target/
*.o
*.wasm
